<%= render :partial => 'admin/shared/nav' %>

<h1>Listing announcements</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Start datetime</th>
    <th>End datetime</th>
  </tr>

<% @announcements.each do |announcement| %>
  <tr>
    <td><%=h announcement.title %></td>
    <td><%=h announcement.description %></td>
    <td><%=h announcement.start_datetime %></td>
    <td><%=h announcement.end_datetime %></td>
    <td><%= link_to 'Show', [:admin, :canada, announcement] %></td>
    <td><%= link_to 'Edit', edit_admin_canada_announcement_path(announcement) %></td>
    <td><%= link_to 'Destroy', [:admin, :canada, announcement], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New announcement', new_admin_canada_announcement_path %>
